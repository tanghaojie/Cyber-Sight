# AI 协作记录：移除岗位编码设计字段

status: completed
date: 2026-08-07

## 用户目标

维护者确认岗位管理不需要岗位编码，要求从当前岗位模块设计中移除该字段。

## 关键指令与假设

- 只修订设计文档，不实现代码、契约或迁移。
- 保留岗位按单部门归属、同部门名称唯一、用户多岗位和岗位不参与授权的既有方案。
- 工作区无其他未提交修改；首次修改前暂存区为空。

## 方案选择

- 删除 `sys_positions.code`、编码相关 API 筛选、唯一索引、失败模式和测试要求。
- 岗位名称在同一部门的有效记录中保持唯一；不同部门允许同名。
- 不引入替代编码字段；未来若需要外部稳定标识另行设计。

## 验证结果

岗位编码字段、筛选、唯一性、失败模式和测试要求已从现行设计中移除；`pnpm format`、`pnpm format:check` 和 `git diff --check` 均通过。未运行代码、契约或数据库测试，因为本次只修订设计文档。

## 未决问题

无。
