---
title: Geo 前端工作台实施计划
scope: platform
repository: Cyber-Sight
owner: project maintainers
date: 2026-08-14
status: completed
---

# Geo 前端工作台实施计划

## 目标

在 Cyber-Sight 中交付一个现代化、地图优先的纯前端 Geo 页面，以新的插件和交互生命周期架构迁移 `vue3-cesium-typescript-start-up-template` 中仍有价值的通用能力。

本计划以 [Geo 前端空间可视化工作台](../../design/modules/geo.md) 和 [Geo 前端编译期插件架构](../../decisions/ADR-20260814-geo-frontend-plugin-architecture.md) 为依据。

## 范围

- `apps/frontend/src/platform/modules/geo/` 中的页面注册、Viewer 生命周期、纯 Cesium 工具、插件适配和 Vue 组件；
- Forge 菜单管理、`registerViews.ts` 和动态 `/geo` 路由的既有装配链路；
- 地图全屏布局和现代化工作台 UI；
- 旧项目通用视图、数据、环境、标绘、测量、模型与地形分析能力的迁移；
- 前端静态验证与维护者人工浏览器验收。

## 非目标

- 后端、API 契约、数据库、场景保存、用户或管理功能；
- Geo 专属功能权限、数据权限和新的菜单管理机制；
- 用户侧 AI、模型提供商、提示词和自然语言地图操作；
- 远程插件、插件市场和用户上传代码；
- 旧项目行业大屏等特定应用；
- 前端自动化、端到端或浏览器测试。

## 实施原则

- 先交付可用工作台骨架和正确生命周期，再逐组迁移能力；
- 每个阶段形成可人工验收的页面，不一次性复制旧项目；
- 旧功能先核对依赖、数据许可和清理行为，再决定复用算法或重写；
- 新增目录和公共端口以当前真实消费者为准，不预建空架构；
- UI 以任务流和上下文组织，功能迁移不等于复刻旧 Ribbon。

## 当前实施切片

首轮先交付可运行垂直切片；维护者确认方向后，本计划继续完成编译期插件内核与全部通用功能：

- [x] 锁定 Cesium 和 Vite 静态资源依赖并完成开发/生产构建装配；
- [x] 通过 `registerViews.ts` 登记 Geo 页面并实现页面级 Viewer 生命周期；
- [x] 实现审定视觉稿中的顶部栏、任务轨、测量上下文面板、地图控制和状态条；
- [x] 提供真实相机复位、2D/3D、全屏以及初始化/失败/重试状态；
- [x] 以距离测量垂直切片验证纯工具、controller、Shell 与 Vue UI 边界；
- [x] 完成允许的静态验证并记录维护者人工浏览器验收边界。

维护者在 2026-08-17 将本地 Node 升级为 `24.19.0`，并要求继续完成计划内剩余功能。本轮在既有活动计划内新增以下实施约束：

- 升级到当前稳定版 `cesium@1.144.0`，重新验证静态资源和生产构建；
- 以旧项目 `imagery-sources.ts` 的源类型为底图需求依据，但不复制其中硬编码令牌；
- 天地图令牌进入公开客户端环境配置，高德 GCJ-02 源必须处理或明确坐标偏移；
- 完成插件内核以及数据、视图、场景、标绘、测量、三维模型、地形分析和对比任务组，并统一接入审定工作台。

## 阶段 1：路由、Viewer 与页面骨架

- [x] 确认当前 Vite、Vue、TypeScript、Node 和浏览器基线兼容的稳定 Cesium 版本并锁定依赖；
- [x] 配置开发与生产环境所需的 Cesium Worker、Widget、字体和 Asset 路径；
- [x] 新建 `platform/modules/geo/`，通过 `registerViews.ts` 登记稳定组件键 `geo`；
- [ ] 在 Forge 菜单管理中配置 `/geo`、组件 `geo`、空布局和空功能权限，不新增静态路由或菜单 migration；
- [x] 确认动态路由直接渲染 Geo 页面，页面不提供返回 Platform、首页或后台的入口；
- [x] 实现单页面实例所有的 `GeoRuntime`、`viewerAccess: GeoViewerAccess` 和页面进入/离开清理；
- [x] 实现 `provideGeoRuntime()` 和返回真实 `Cesium.Viewer` 的 `useCesiumViewer()`；
- [x] 让 `GeoPluginContext.viewer` 和纯工具的 `viewer` 参数统一为 `Cesium.Viewer`；
- [x] 禁止把 Viewer 放入 `window`、Vue 全局属性或 Pinia；
- [x] 实现 Geo 工作台基础布局、WebGL 不可用、初始化中、失败与重试状态；
- [x] 确认不新增后端、契约、migration 或权限定义。

完成条件：Forge 菜单动态加载 `/geo`；页面不包含返回 Platform 的入口；开发与生产构建均可创建 Viewer；重复进入页面不会创建残留实例。

## 阶段 2：UI 系统与插件内核

- [x] 实现 48–56px 顶栏、左侧工具轨、按需上下文面板、右侧属性检查器和底部状态条；
- [x] 使用现有 Sight 设计令牌、图标和本地化能力；
- [x] 建立 `tools/` 纯 Cesium 工具边界，禁止依赖 Vue、Pinia、UI、本地化和插件注册表；
- [x] 使用现有 lint/架构检查和人工 diff 复核 `tools/` 的依赖方向，并阻止 Shell 穿透导入具体工具；需要通用工具扩展时先走 Forge 上游流程；
- [x] 建立“纯工具 → 插件 controller/UI contributions → 插件自有 `.vue`”调用链；
- [x] 为有状态工具定义 session、`stop()`/`dispose()` 和工具/交互 scope 所有权；
- [x] 定义插件 ID、依赖、排序、`GeoPluginContext`、带 UI 贡献的安装实例和错误隔离；
- [x] 实现重复 ID、缺失依赖和依赖循环校验，并按拓扑顺序安装、逆序销毁；
- [x] 实现插件独立 `DisposableScope`、`AbortController` 和统一 Cesium 资源登记辅助方法；
- [x] 实现 `InteractionManager`，保证鼠标主交互工具互斥；
- [x] 区分 action、toggle、panel 和 interaction 工具，统一交互的激活、完成、取消和幂等销毁协议；
- [x] 实现最小类型化 capability registry 和 event bus，禁止插件穿透导入其他插件；
- [x] 完成桌面、窄桌面和手机基础降级布局；
- [x] 为图标按钮、焦点、活动状态、工具提示和 reduced motion 补齐首版无障碍行为；
- [ ] 对 1280×720 进行重点人工视觉验收，确保地图面积和信息层级明显优于旧站。

完成条件：插件可贡献工具与面板；纯工具可脱离 Vue 使用；代码中的 `viewer` 均为 `Cesium.Viewer`，生命周期访问器明确命名为 `viewerAccess`；切换任务组不重建 Viewer；页面不出现旧式 Ribbon 和永久宽侧栏。

## 阶段 3：视图、数据与场景能力

- [x] 迁移全球/中国定位、相机坐标、鼠标坐标、相机高度和 FPS/状态信息；
- [x] 实现 2D、3D、哥伦布模式、相机设置和视距限制；
- [x] 建立影像、地形、模型和 3D Tiles 的前端预置配置；
- [x] 实现预置数据的显示、隐藏、定位、排序或适用的局部属性调整；
- [x] 迁移太阳、月亮、大气、光照、天空盒、阴影、地球底色和深度检测；
- [x] 登记数据许可、浏览器令牌、CORS 和生产可用性限制，不提交维护者私有凭据；
- [x] 让单数据源失败保持局部，不影响 Viewer 和其他数据。

完成条件：用户能通过任务面板完成基础浏览、数据切换和环境调整；预置数据在生产构建中可用或明确显示不可用原因。

## 阶段 4：标绘、测量与三维模型工具

- [x] 迁移点、线、面标绘以及当前/全部结果清理；
- [x] 迁移点位、距离和面积测量以及结果清理；
- [x] 迁移 3D Tiles 高亮、滑动/点击分类；
- [x] 迁移模型切割或分屏、偏移校正；
- [x] 为每个交互工具实现取消、完成、错误和切换清理；
- [x] 通过统一 scope、幂等销毁和最终 diff 复核连续切换、重复激活和离开路由的资源归属；
- [x] 在状态条或浮动提示中显示当前工具、操作步骤和退出方式。

完成条件：绘制、测量和模型工具可直接供用户使用；任意时刻只有一个鼠标主交互工具；工具退出不污染其他图层和插件。

## 阶段 5：地形分析、对比与功能收口

- [x] 迁移地形采样、淹没分析、等高线和地形着色；
- [x] 迁移影像或场景分屏对比；
- [x] 为耗时分析限制输入规模、提供进度/取消反馈并保持界面可响应；
- [x] 建立旧站通用能力对照表，记录已迁移、替代、推迟和废弃原因；
- [x] 统一所有任务组的名称、图标、面板密度、空状态和错误提示；
- [ ] 完成 1280×720、常用桌面宽度和窄屏降级的人工视觉与交互验收；
- [ ] 完成长时间操作、连续进入/退出和外部数据失败的人工资源生命周期验收。

完成条件：设计列出的通用旧能力均有明确结论；已实现功能在统一工作台中可用；视觉和交互不再继承旧站主要问题。

## 阶段 6：最终验证与文档归档

- [x] 更新 Geo 设计和 ADR，使其描述最终实现和实际公共接口；
- [x] 记录 Cesium 版本、数据源、许可、运行时配置和人工验收边界；
- [x] 运行适用的格式、类型、lint、构建、架构和文档门禁；
- [x] 明确前端静态检查不能替代维护者人工功能验收；
- [x] 更新计划和 AI 日志的实际偏差、验证结果、遗留问题和关联提交；
- [x] 将完成计划和 AI 日志归档并更新索引；
- [x] 按维护者明确授权忽略当前归档审计 `DUE`，由本文件所在交付提交创建并验证真实 AI 模型 trailer。

## 验证命令

每阶段执行与改动相称的局部检查，最终至少执行：

```text
pnpm format
pnpm format:check
pnpm lint
pnpm build
pnpm architecture:check
pnpm docs:archive:check:ci
```

按仓库规则不创建或运行前端自动化、端到端和浏览器测试。人工验收以设计文档中的清单和各阶段完成条件为准。

## 风险与控制

- Cesium 资源路径在开发和生产环境不一致：阶段 1 同时验证两种构建，不延后到功能迁移结束；
- Viewer 被全局缓存或异步任务持有过久：只允许当前页面运行时拥有实例，异步流程同时绑定 `AbortSignal`；
- 旧算法依赖全局状态：逐个能力解耦，通过真实 Viewer 参数、纯工具和插件上下文接入；
- 事件和临时资源泄漏：以插件拥有权、互斥工具和幂等销毁作为完成门槛；
- 外部数据受 CORS、令牌、限频和许可影响：只发布明确允许且可被浏览器安全访问的数据；
- 为插件化创建过度抽象：只实现当前任务组共同需要的最小端口；
- UI 再次被功能数量淹没：首层按用户任务分组，高级参数按需展开，并以 1280×720 人工验收作为门槛。

## 实际偏差与遗留问题

原始垂直切片使用 Node `20.19.4`、`cesium@1.140.0` 和 Natural Earth II；维护者在 2026-08-17 把 Node 升级到 `24.19.0`，本轮据此升级到 `cesium@1.144.0` 并完成全部通用任务组。底图改用旧项目的多源能力模型，但硬编码令牌未进入仓库；Natural Earth II 仍作为离线兜底层，高德 GCJ-02 源默认禁用并明确原因。完整构建、类型、格式、lint 和架构检查通过。Forge 菜单记录仍需要维护者配置；1280×720、连续交互、路由进出、外部服务可达性和 WebGL 资源行为按仓库规则保留为维护者人工验收，未被静态检查替代。

## 关联记录

- [Geo 前端空间可视化工作台](../../design/modules/geo.md)
- [Geo 前端编译期插件架构](../../decisions/ADR-20260814-geo-frontend-plugin-architecture.md)
- [Geo 模块设计协作记录](../ai-logs/2026/08/2026-08-14-geo-platform-design.md)

代码实现和前端验证已经完成。`pnpm docs:archive:check:ci` 因归档审计的 Foundation/Platform 作用域设计缺口返回 `DUE`；维护者于 2026-08-18 明确授权本次忽略该检查并提交，随后在 Forge 项目修正审计设计。该例外不表示门禁已通过，活动归档审查记录继续保留。
