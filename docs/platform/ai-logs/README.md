# Platform 活动 AI 协作记录

当前没有活动的 Platform AI 协作记录。

业务平台使用公共[AI 日志模板](../../templates/ai-session-log-template.md)，并设置 `scope: platform`。新日志保存到 `<change-type>/YYYY/MM/`，其中类型只能是 `chore`、`docs`、`feat`、`fix`、`refactor`、`style`、`test`、`ci`、`build` 或 `revert`；历史日志保持原路径。下游同步会保留本目录，维护者需在各下游仓库执行同等更新。
