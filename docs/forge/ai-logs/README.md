# Forge 活动 AI 协作记录

Forge 专属协作使用公共[AI 日志模板](../../templates/ai-session-log-template.md)，并设置 `scope: forge`。新日志保存到 `<change-type>/YYYY/MM/`，其中类型只能是 `chore`、`docs`、`feat`、`fix`、`refactor`、`style`、`test`、`ci`、`build` 或 `revert`；历史日志保持原路径。当前没有活动记录。
