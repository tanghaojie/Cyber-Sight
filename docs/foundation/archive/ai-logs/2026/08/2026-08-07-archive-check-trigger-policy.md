# AI 协作记录：按任务范围触发文档归档审查

status: completed
date: 2026-08-07

## 用户目标

用户要求先恢复归档检查的执行环境，再把每次 AI 启动强制检查改为按任务范围触发，并增加合并前自动化兜底。

## 关键指令与假设

- 保留现有归档阈值和立即触发条件，只调整检查时机。
- 只读问答、代码浏览、格式化、注释和单文件机械改动不触发归档审计。
- 涉及业务行为、API、数据模型、模块、架构、ADR、迁移或文档治理的任务触发审计。
- 不把本机或 AI 平台私有状态作为跨 AI 协作协议。

## 方案选择

使用仓库原生的任务范围规则，并新增 `docs:archive:check:ci` 入口。没有 `.github/` 或其他已登记 CI 提供方，因此不新增供应商特定工作流文件。

## 实际改动

- 将 `AGENTS.md`、`docs/README.md`、`docs/plans/README.md` 和文档治理设计改为任务范围触发。
- 新增 ADR-0033，归档并标记 ADR-0030 为 superseded。
- 增加 `docs:archive:check:ci` 包脚本。
- 更新归档台账、归档索引、完成计划和 AI 日志。

## 验证结果

- 首次修改前暂存区为空，职位模块提交后工作区只剩本任务文件。
- 默认 Node 20 在受限沙箱中因 `C:\Users\thj_3` 父目录解析权限返回 `EPERM`。
- 使用工作区 Node 24 运行归档脚本成功。
- 依赖安装首次被残留 esbuild 进程占用中断；终止该进程、清理可再生 `node_modules` 后，`pnpm install` 成功。
- `pnpm format` 和 `pnpm format:check` 通过。
- 归档审计确认无 broken links，CI 入口返回 0。
- 归档台账基线更新为职位模块提交后的 `ba0657a22b8531c4d9d4f1fda859e31a741cd9b4`。

## 未决问题

系统 Node 20 的沙箱权限问题不能通过仓库配置安全修复；项目不写入机器特定运行时路径。当前验证使用工作区运行时完成。
